{
  "name": "pptrmocha",
  "version": "1.0.0",
  "description": "Puppeteer with mocha",
  "main": "index.js",
  "scripts": {
    "test": "mocha --timeout=30000 --require mocha-steps ./build/tests/**.js",
    "clean": "rm -rf build",
    "build": "babel --preset es2015 -d build/ src/"
  },
  "author": "Praveen Kumar Naarajan",
  "license": "ISC",
  "dependencies": {
    "chai": "^4.2.0",
    "mocha": "^8.0.1",
    "mocha-steps": "^1.3.0",
    "puppeteer": "^5.2.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  }
}
